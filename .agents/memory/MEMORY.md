# Memory Index

- [Production VPS deploy quirks](prod-vps-deploy.md) — user self-hosts via pm2 pulling branch `dev`; DATABASE_URL must be exported for db push, pg_trgm needs one-time enable, deleted attached_assets break builds.
